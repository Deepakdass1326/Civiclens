# Development (run the backend on localhost:5000)
VITE_API_BASE_URL=http://localhost:5000/api

# Production on the same Vercel deployment (recommended)
# VITE_API_BASE_URL=/api

# Production with a separate backend service
# VITE_API_BASE_URL=https://your-backend-service.example.com/api
